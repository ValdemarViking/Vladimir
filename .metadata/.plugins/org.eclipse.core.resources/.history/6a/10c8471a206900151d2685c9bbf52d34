import java.util.Scanner;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class EightLesson {

	public static void main(String[] args) {
	/*	Необходимо сделать регулярку, которая сможет проверять телефонный номер,  
	 * но не просто телефонный номер, а еще принадлежит ли номер Беларуси. 
	 *	Короткие номера и так далее пропускаем, нас интересует только номер 
	 *в стиле +375 29 555-55-55.
		Это задание можно сделать в рамках следующего если не успеваете.*/

		String telNumber = new String();
		Scanner in = new Scanner (System.in);
		
		System.out.println("Введите телефонный номер");
		telNumber = in.nextLine();
				
		Pattern p = Pattern.compile("[+375]{1}+[29||33||44]+d{7}");
		Matcher m = p.matcher(telNumber);
		boolean b = m.matches();
		if (b = true) {
			System.out.println("Телефонный номер верен");
		}
			else 
			System.out.println("Телефонный номер не верен");
		}
		
		
	

}
